services:
  - type: web
    name: rag-pipe-line-demo
    env: docker
    branch: main
    dockerfilePath: Dockerfile
    plan: starter
    autoDeploy: true
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
